import { Route } from '@angular/router';
import { ImamRegistrationComponent } from './imam-registration.component';

export const ImamRegistrationRoutes: Route[] = [
    {
        path     : '',
        component: ImamRegistrationComponent
    }
];
