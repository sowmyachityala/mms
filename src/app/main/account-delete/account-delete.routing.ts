import { Route } from '@angular/router';
import { AccountDeleteComponent } from './account-delete.component';

export const AccountDeleteRoutes: Route[] = [
    {
        path     : '',
        component: AccountDeleteComponent,
    }
];
