import { Route } from '@angular/router';
import { MosqueOnboardingComponent } from './mosque-onboarding.component';

export const MosqueOnboardRoutes: Route[] = [
    {
        path     : '',
        component: MosqueOnboardingComponent
    }
];
